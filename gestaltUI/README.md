# Design the interaction flow and UI...

This is a code bundle for Design the interaction flow and UI.... It's based on default Vite project. The original design is available at https://app.flowstep.ai/file?activeFileId=4b4aa8f3-2d11-42b0-a87e-e3d97c652bb5.

## Running the code

Run `npm i` and `npm run setup` to install the dependencies.

Run `npm run dev` to start the development server.
